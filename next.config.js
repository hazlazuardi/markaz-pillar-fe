module.exports = {
  reactStrictMode: true,
  env: {
    BACKEND_HOST: process.env.BACKEND_HOST,
  },
  images: {
    domains: ['source.unsplash.com'],
  },
  env: {
    BACKEND_HOST: process.env.BACKEND_HOST,
  }

}
